<div id='gallery'>
  <h2><%= @galleries.first.title %> <%= h2 %></h2>

  <div id="galleria">
    <% @galleries.each do |gallery| -%>
      <a href="<%= gallery.image %>"><img data-title="<%= gallery.title %>" data-description="<%= gallery.description unless gallery.description.blank? %>" src="<%= gallery.image %>" /></a>
    <% end -%>
  </div>
</div>