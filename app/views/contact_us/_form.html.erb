<%= form_for(@contact_us) do |f| %>
  <% if @contact_us.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@contact_us.errors.count, "error") %> prohibited this contact_us from being saved:</h2>

      <ul>
      <% @contact_us.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :email %><br />
    <%= f.text_field :email %>
  </div>
  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
